%% Compute sum
%%
sum=0
for i=1:20
    sum=sum+power(i,4)+1
end
disp(sum)

%% Roots of Polynomial
%%
p = [1 0 0 -10 0 0 2]
r = roots(p)
disp(r)

%% Quadratic equation
%%
p = [26 5 -7]
r = roots(p)
disp(r)

%% Square Root
%%
sq = sqrt(26)
disp(sq)

